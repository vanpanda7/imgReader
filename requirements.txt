# ==================== JMComic 核心依赖 ====================
curl_cffi
commonX
PyYAML
Pillow
pycryptodome

# ==================== FastAPI 服务依赖 ====================
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0

# ==================== Redis 缓存（可选，但推荐）====================
redis>=5.0.0
hiredis>=2.2.0  # Redis 性能优化库（可选）

# ==================== JMComic 可选功能依赖 ====================
# 以下依赖为可选，未安装不影响基本功能
# psutil  # 用于系统资源监控（可选）
# plugin_jm_server  # 用于内置服务器功能（可选）
# zhconv  # 用于繁简转换（可选）
